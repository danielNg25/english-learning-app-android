<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.ndtr.mylearningenglish">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_192"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme.NoActionBar">
        <activity android:name=".activities.FourAnswerExerciseActivity"></activity>
        <activity
            android:name=".activities.FillBlankExerciseActivity"
            android:windowSoftInputMode="adjustPan" />
        <activity android:name=".activities.ExercisesListActivity"
            android:parentActivityName=".activities.TopicActivity"/>
        <activity android:name=".activities.WordsListActivity"
            android:parentActivityName=".activities.TopicActivity"/>
        <activity android:name=".activities.TopicActivity" />
        <activity android:name=".activities.MainScreenActivity" />
        <activity android:name=".activities.WordActivity" />
        <activity android:name=".activities.SignUpActivity"
            android:theme="@style/AppTheme.NoActionBar">

        </activity>
        <activity android:name=".activities.LoginActivity"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>